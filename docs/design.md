# リキッ碁 / Liqui-go — ゲームデザイン

## コンセプト

「物理の碁石では表現できなかった 0.5 石を、液体で実現した囲碁」。

サッカーを見て「あとから動かせる囲碁」と感じた観察が起点。スペースやエリアへの「効き」を可視化したい、という動機から発展した。

## 世界観

| 要素       | 物質                                   |
| ---------- | -------------------------------------- |
| 1 石       | 石製の枠。完全に固まった構造物         |
| 0.5 石     | まだ固まっていないセメント。柔らかく不安定 |
| 内側の液体 | 白・黒の流体。枠に閉じ込められて溜まる  |

液体の物理比喩で囲碁の状態を表す（確定した地＝海抜0の池、勢力が強い場所＝表面張力で盛り上がった液体、死に石＝水没、など）。

## 石の計算ルール — すべて足し算

黒をプラス、白をマイナスとして加算するだけ。セル値は `{-1, -0.5, 0, 0.5, 1}` の5値。

| 操作                   | 計算             | 結果           | 現象名         |
| ---------------------- | ---------------- | -------------- | -------------- |
| 空きに1を打つ          | `0 + 1 = 1`      | 1石            | place          |
| 空きにポアする         | `0 + 0.5 = 0.5`  | 0.5石          | ポア (pour)    |
| 自分の0.5に自分の0.5   | `0.5 + 0.5 = 1`  | 1石に確定      | ソリディファイ |
| 自分の0.5に相手の0.5   | `0.5 - 0.5 = 0`  | 消滅           | ブレイク       |
| 相手の0.5に1を打つ     | `-0.5 + 1 = 0.5` | 自分の0.5      | オーバーポア   |
| 相手の1にポアする      | `-1 + 0.5 = -0.5`| 相手の0.5へ削る| reduce         |
| 相手の1に1を打つ       | `-1 + 1 = 0`     | 両方消滅       | capture        |

> **未解決（要design確認）**: notes 原案では「相手の0.5にポアする → 0.5 オーバーポア」と「自分の0.5に相手の0.5 → 0 ブレイク」が併記されているが、どちらも "0.5 vs 相手の0.5" の同一操作に見える。前者はデルタが `+1`（1石ぶん）になっており、ポア(0.5)ではなく「1石で上書き」を指している疑いが強い。現状の実装は **デルタの大きさで分岐**（|1|=オーバーポア、|0.5|=ブレイク）している。正式ルールは未確定。

## 3つのルールセット（絡み合って成立する）

液体表示・境界線・3D 地形は**表示の改善**であってルール変更ではない。

### ルール① 同時プロット制

先手・後手なし。両者が次の手を同時に公開する。コミ不要（先手有利が構造的に存在しない）。衝突は全て足し算で処理。

### ルール② 1.5手（星パックマン）

星・天元に置いた瞬間、1.5 手の権利を獲得（ストック、上限 1.5）。星の石が取られても権利は消えない。相手が同じ星に置けば権利は上書き。星は何度でも取り合える権利ポイント。

### ルール③ 0.5石が動ける（ムーブ）

0.5 石だけが動ける（1 石は動かせない）。移動は通常の 1 手。移動範囲は隣接 8 マス。動かしたターンは置けない。移動先の結果も足し算。盤上の 0.5 石すべてが常に脅威になる。

**同時ムーブの特殊ケース**: 両者が同じ場所へ → トリス（3箇所同時に空く）／互いの石へ → スワップ（入れ替わり）。

### 連続配置禁止（コウ相当）

直前のターンに置いた・動かした場所には次のターン置けない（2 ターン後に解放）。相殺ループ防止。

## 現象の命名

ポア / ソリディファイ / オーバーポア / ブレイク / ムーブ / スワップ / プール / リーク / チャージ / トリス。略称は自然発生に任せ、公式は正式名で統一。

## デジタルならではの解決

- 物理碁のコミ（五目半）は「石が整数しか存在できない」制約への補正。リキッ碁は 0.5 石が自然に存在 → コミ不要。
- 地の量を液体量としてリアルタイムに可視化。
- **スコアは m³ 単位**（1 マス = 1m³）。「何目」ではなく「何 m³」。

## 技術方針

- Web アプリ。Three.js で 3D 地形レンダリング。
- 各交点の「確定度」をスコアリングして heightmap に変換。
- 石を置くたび地形がリアルタイムに変形。液体シミュレーションはシェーダーで表現。

## ロードマップ

- [x] コンセプト確定
- [x] リポジトリ作成 / 雛形（盤面レンダリング・足し算エンジン・GameState）
- [ ] オーバーポアのルール確定（上記「未解決」）
- [ ] クリック着手・合法手判定（コウ／連続配置禁止）
- [ ] 同時プロット制（ルール①）
- [ ] 1.5手の権利（ルール②）
- [ ] ムーブ／スワップ／トリス（ルール③）
- [ ] 確定度スコアリング → heightmap
- [ ] 液体地形レンダリング（シェーダー）
- [ ] スコア表示（m³）
