{
  "name": "liqui-go",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "author": "kako-jun",
  "description": "リキッ碁 (Liqui-go) — Go reimagined with liquid. The 0.5 stone realized as fluid.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@types/three": "^0.184.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.9"
  }
}
